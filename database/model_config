create table public.model_config (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  type text null default 'image'::text,
  name text null default 'nano-banana-pro'::text,
  config json null default '{  "model_version": "google/nano-banana-pro",  "resolution": "2K",  "aspect_ratio":"16:9",  "output_format": "png",  "safety_filter_level": "block_only_high"                      }'::json,
  "isActive" boolean null default true,
  constraint model_config_pkey primary key (id)
) TABLESPACE pg_default;